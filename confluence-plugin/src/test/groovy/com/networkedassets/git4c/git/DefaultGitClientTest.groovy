package com.networkedassets.git4c.git

import spock.lang.Specification

class DefaultGitClientTest extends Specification {

/*

    def client = new DefaultGitClient()
    def usernameField = new SourceSettingField("username", "username", "admin", SourceSettingField.Type.TEXT)
    def settings = [username: usernameField]
    def sourceUnitIdentifier = new SourceUnitIdentifier("http://bitbucket.networkedassets.net/scm/project/repo.git")
    def sourceData = new SourceData(sourceUnitIdentifier, settings)


    def "should create correct repositoryPath basing on sourceData formatted identifier"() {

        when:


        def repositoryPath = client.getRepositoryPath(sourceData)


        then:
        repositoryPath == "http://admin@bitbucket.networkedassets.net/scm/project/repo.git"
    }


    def "should create correct repositoryPath basing on sourceData unformatted identifier"() {

        sourceUnitIdentifier = new SourceUnitIdentifier("http://admin@bitbucket.networkedassets.net/scm/project/repo.git")
        sourceData = new SourceData(sourceUnitIdentifier, settings)

        when:


        def repositoryPath = client.getRepositoryPath(sourceData)


        then:
        repositoryPath == "http://admin@bitbucket.networkedassets.net/scm/project/repo.git"
    }


    def "should create correct url basing on sourceData formatted identifier "() {

        when:

        def repositoryPath = client.getUrl(sourceData)


        then:
        repositoryPath == "http://bitbucket.networkedassets.net/"
    }


    def "should create correct url basing on sourceData unformatted identifier "() {

        sourceUnitIdentifier = new SourceUnitIdentifier("http://admin@bitbucket.networkedassets.net/scm/project/repo.git")
        sourceData = new SourceData(sourceUnitIdentifier, settings)

        when:

        def repositoryPath = client.getUrl(sourceData)


        then:
        repositoryPath == "http://bitbucket.networkedassets.net/"
    }


    def "should create correct formatted sourceData identifier basing on sourceData formatted identifier "() {


        when:

        def formattedIdentifier = client.getFormattedSourceUnitIdentifier(sourceData).toString()


        then:
        formattedIdentifier == "http://bitbucket.networkedassets.net/scm/project/repo.git"
    }


    def "should create correct formatted sourceData identifier basing on sourceData unformatted identifier "() {

        sourceUnitIdentifier = new SourceUnitIdentifier("http://admin@bitbucket.networkedassets.net/scm/project/repo.git")
        sourceData = new SourceData(sourceUnitIdentifier, settings)

        when:

        def formattedIdentifier = client.getFormattedSourceUnitIdentifier(sourceData).toString()


        then:
        formattedIdentifier == "http://bitbucket.networkedassets.net/scm/project/repo.git"
    }
*/

}
